<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TabLayout_Slider_SearchInRecyclerView"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
<!--        HomeActivity viewpage2 - circleindicator3-->
        <activity
            android:name=".activity.HomeActivity"
            android:exported="false" />
<!--         PromotionActivity viewpage- circleindicator-->
        <activity
            android:name=".activity.PromotionActivity"
            android:exported="false" />
<!--                 ViewFlipperActivity flipper -->
        <activity
            android:name=".activity.ViewFlipperActivity"
            android:exported="false" />
<!--         ShopeeActivity search in recycler view-->
        <activity
            android:name=".activity.ShopeeActivity"
            android:exported="false" />
<!--        MainActivity tablayout-->
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>